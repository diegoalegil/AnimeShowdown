# URL pública del backend Spring Boot — opcional
# Si se omite, src/lib/api.js usa por defecto la URL de Railway en producción.
# Para desarrollo local apuntando a tu Spring Boot:
#   VITE_API_URL=http://localhost:8080
VITE_API_URL=
