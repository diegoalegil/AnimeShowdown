# URL pública del backend Spring Boot — obligatoria en producción
# En producción usa SIEMPRE el subdominio API, no el dominio bruto de Railway:
# OAuth y refresh dependen de cookies en api.animeshowdown.dev.
# Para desarrollo local apuntando a tu Spring Boot:
#   VITE_API_URL=http://localhost:8080
VITE_API_URL=
