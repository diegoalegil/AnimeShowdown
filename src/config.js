// Reglas de la colección. Cambiar aquí cambia el juego entero.

/** Sobres que se pueden abrir cada día (el día es el local del navegador). */
export const SOBRES_POR_DIA = 5

/** Cartas que trae cada sobre. */
export const CARTAS_POR_SOBRE = 5

/** Probabilidad de que la última carta del sobre sea especial. */
export const PROB_ESPECIAL = 0.15

/** Clave de localStorage y versión del formato guardado. */
export const CLAVE_ALMACEN = 'animeshowdown:v1'
export const VERSION_ALMACEN = 1
