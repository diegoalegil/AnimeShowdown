<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.15</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.diegoalegil</groupId>
	<artifactId>animeshowdown</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>AnimeShowdown</name>
	<description>API REST de votación y ranking de popularidad de personajes anime</description>
	<url>https://github.com/diegoalegil/AnimeShowdown</url>
	<licenses>
		<license>
			<name>MIT License</name>
			<url>https://opensource.org/licenses/MIT</url>
		</license>
	</licenses>
	<developers>
		<developer>
			<id>diegoalegil</id>
			<name>Diego Gil</name>
			<email>diegogildam@gmail.com</email>
		</developer>
	</developers>
	<scm>
		<connection>scm:git:git://github.com/diegoalegil/AnimeShowdown.git</connection>
		<developerConnection>scm:git:ssh://github.com:diegoalegil/AnimeShowdown.git</developerConnection>
		<url>https://github.com/diegoalegil/AnimeShowdown/tree/main</url>
	</scm>
	<properties>
		<java.version>21</java.version>
		<jacoco.version>0.8.15</jacoco.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>
		<dependency>
			<groupId>com.auth0</groupId>
			<artifactId>java-jwt</artifactId>
			<version>4.5.2</version>
		</dependency>
		<dependency>
			<groupId>nl.martijndwars</groupId>
			<artifactId>web-push</artifactId>
			<version>5.1.2</version>
		</dependency>
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpasyncclient</artifactId>
			<version>4.1.5</version>
		</dependency>
		<dependency>
			<groupId>org.bitbucket.b_c</groupId>
			<artifactId>jose4j</artifactId>
			<version>0.9.6</version>
		</dependency>
		<dependency>
			<groupId>org.bouncycastle</groupId>
			<artifactId>bcprov-jdk18on</artifactId>
			<version>1.84</version>
		</dependency>
		<!-- Lombok: @Getter/@Setter en entidades JPA para recortar boilerplate.
		     scope provided + optional => NO se empaqueta en el jar; solo procesa
		     anotaciones en compilacion. Version pineada a la cacheada en ~/.m2
		     para que el build offline (mvn -o) la resuelva. NUNCA @Data en
		     entidades JPA (equals/hashCode + lazy = stack overflow): solo
		     @Getter/@Setter, y las entidades con getters de logica propia
		     conservan su metodo manual (Lombok no duplica el ya existente). -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>1.18.46</version>
			<scope>provided</scope>
			<optional>true</optional>
		</dependency>
		<!-- Cache (Caffeine in-memory) + Resilience4j para JikanService:
		     retry, circuit breaker, timeout y caché sobre llamadas a Jikan API.
		     spring-boot-starter-aop es transitive de resilience4j-spring-boot3
		     pero lo declaramos explícito para evitar sorpresas. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<dependency>
			<groupId>io.github.resilience4j</groupId>
			<artifactId>resilience4j-spring-boot3</artifactId>
			<version>2.4.0</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aop</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.8.5</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>provided</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- TwelveMonkeys ImageIO plugin para leer webp en BufferedImage.
		     Java SE no soporta webp por defecto y las imágenes del catálogo
		     están en webp; también se usa para componer OG images dinámicas. -->
		<dependency>
			<groupId>com.twelvemonkeys.imageio</groupId>
			<artifactId>imageio-webp</artifactId>
			<version>3.13.1</version>
		</dependency>

		<!-- Bucket4j para rate limiting in-memory. Protege /auth/login,
		     /auth/registro, /auth/forgot, /auth/reset
		     y /enfrentamientos/*/votar contra ataques de fuerza bruta y
		     spam de votos. Bandwidth compuesto: 5/min + 50/hora por IP. -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j-core</artifactId>
			<version>8.10.1</version>
		</dependency>

		<!-- Flyway para migraciones de schema versionadas. Reemplaza al
		     hibernate ddl-auto=update + SchemaMigrations.java
		     manual. Cada cambio futuro = un V{n}__{descripcion}.sql en
		     resources/db/migration. -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<!-- springdoc-openapi 2.8.5 declarada arriba — disponible en
		     /v3/api-docs y /swagger-ui.html. El duplicado previo con
		     versión 2.7.0 se eliminó: Maven respeta el primer declarado
		     pero el warning de "duplicate" amenazaba estabilidad futura. -->

		<!-- spring-retry para @Retryable + @Recover en EmailService
		     aspectjweaver lo arrastra spring-boot ya. -->
		<dependency>
			<groupId>org.springframework.retry</groupId>
			<artifactId>spring-retry</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-aspects</artifactId>
		</dependency>

		<!-- TOTP: generación de secrets base32, validación de códigos
		     de 6 dígitos con drift de ±1 step (30s cada uno),
		     y construcción de la URI otpauth:// que codificamos a QR
		     para que el user la escanee con Google Authenticator, Authy,
		     1Password, etc. Lib pura Java sin dependencias externas. -->
		<dependency>
			<groupId>dev.samstevens.totp</groupId>
			<artifactId>totp</artifactId>
			<version>1.7.1</version>
		</dependency>

		<!-- WebSocket + STOMP. Spring Boot starter trae spring-websocket
		     + spring-messaging + jackson-jsr310. Endpoint
		     STOMP en /ws con handshake JWT-autenticado; broker simple
		     in-memory para /topic (broadcast público de brackets) y
		     /user/queue (notificaciones personales). Cliente frontend:
		     @stomp/stompjs (~6KB gzip). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-websocket</artifactId>
		</dependency>
	</dependencies>
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- Mockito inline mock-maker hace self-attach del ByteBuddy
			     agent en runtime para poder
			     mockear final classes/methods. En JDK 21+ esto genera
			     warning ("Mockito is currently self-attaching") y en
			     JDK 24+ está bloqueado por default a menos que pases
			     -XX:+EnableDynamicAgentLoading o -javaagent explícito.

			     Si el JDK del runner no permite self-attach (e.g. CI
			     con flags restrictivos), todos los tests fallan en
			     arranque. La forma portable es declarar byte-buddy-agent
			     como dependencia explícita y pasarlo a Surefire como
			     -javaagent — Mockito lo detecta y skipea el self-attach.

			     dependency-plugin resolve la ruta del jar en local
			     repo y la expone como property; Surefire la inyecta
			     vía argLine. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>properties</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>@{argLine} -javaagent:${net.bytebuddy:byte-buddy-agent:jar} -Xshare:off</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>service-coverage-gate</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<!-- Hard gate de coverage para service.*, controller.* y
							     security.*. Umbrales con margen defensivo bajo la cobertura
							     real medida (service ~83/62, controller ~79/56, security
							     ~82/75) para no romper por fluctuaciones ni penalizar PRs
							     no relacionados. controller y security antes no tenían gate. -->
							<haltOnFailure>true</haltOnFailure>
							<rules>
								<rule>
									<element>PACKAGE</element>
									<includes>
										<include>com.diegoalegil.animeshowdown.service*</include>
									</includes>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.65</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.50</minimum>
										</limit>
									</limits>
								</rule>
								<rule>
									<element>PACKAGE</element>
									<includes>
										<include>com.diegoalegil.animeshowdown.controller*</include>
									</includes>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.65</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.45</minimum>
										</limit>
									</limits>
								</rule>
								<rule>
									<element>PACKAGE</element>
									<includes>
										<include>com.diegoalegil.animeshowdown.security*</include>
									</includes>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.70</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.60</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	
</project>
